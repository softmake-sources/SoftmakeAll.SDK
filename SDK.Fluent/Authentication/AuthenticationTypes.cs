namespace SoftmakeAll.SDK.Fluent.Authentication
{
  /// <summary>
  /// Authentication types. Application uses EnvironmentUniqueID. Credentials and Interactive uses the PublicClientApplication
  /// </summary>
  public enum AuthenticationTypes
  {
    Application = 0,
    Credentials = 1,
    Interactive = 2
  }
}